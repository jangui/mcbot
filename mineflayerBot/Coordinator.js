/*
The coordinator handles is the bot's movement manager.
The bot interfaces with the coordinator for movement, orientation & path finding
 */

class Coordinator {
    constructor(bot) {
        this.bot = bot;
    }
}
